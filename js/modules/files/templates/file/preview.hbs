<div id="preview">
  {{name}}
  {{#if isPicture}}
    <img {{bind-attr src=filePath}}></img>
  {{/if}}

  {{#if isMovie}}
    <video {{bind-attr src=filePath}} type="video/mp4" controls>
      Your browser does not support the <code>video</code> element.
    </video>
  {{/if}}
</div>